*NAME&
        kernel - low-level system interfaces

*DESCRIPTION
        kernel& is a global table provided to the init process by the kernel.  It contains such low-level kernel functions as thread and user management.  The *kernel& API provides five sub-fields: the scheduler, *kernel.thread&(*2&); filesystem management through *kernel.filesystem&(*2&); *kernel.logger&(*2&), a basic bootlogger; user management through *kernel.users&(*2&); and module management in *kernel.module&(*2).

*NOTES
        kernel.module& is untested.

*COPYRIGHT
        Monolith Kernel Interfaces& (c) 2020 Ocawesome101 under the GNU GPLv3.
