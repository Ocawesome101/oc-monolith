*NAME&
        readline - read text input from the user

*SYNOPSIS
        local readline = require("readline")

        readline& ( [ *prompt& = ~string&, *n& = #number& ], @{& [ *history& = @table&, *arrows& = ?boolean&, *pwchar& = ~string&, *prompt& = ~string& ] @}& )
